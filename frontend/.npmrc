# npm の engines 検査を「警告」ではなく「エラー」に格上げする。
# package.json の engines（node >=24 <25 / npm >=11 <12）を満たさない環境では
# npm install / npm ci を失敗させ、バージョン差による package-lock.json の
# 意図しない再生成を未然に防ぐ。固定バージョンの導入は Volta 推奨（volta フィールド）。
engine-strict=true
